<h1>#{summonerName}

<table.zebra-striped id=#{champTableId}>
    <thead>
        <tr>
            <th>Champion
            <th>Games Played
            <th>Win %
            <th>Kills / Min
            <th>Deaths / Min
            <th>Assists / Min
            <th>CS / Min
            <th>Gold / Min
    <tbody>
        $forall (champ, stats) <- champData
            <tr>
                <td>#{u2t champ}
                    <td>#{getResultCount stats}
                $forall col <- columns
                    <td>
                        $maybe datum <- getResultValue col stats
                            $with formatted <- forceString (printf "%0.2f" datum)
                                #{formatted}
    <tfoot>
        $forall (title, stats) <- totals
            <tr.total>
                <td>Total
                $forall col <- columns
                    <td>
                        $maybe datum <- getResultValue col stats
                            $with formatted <- forceString (printf "%0.2f" datum)
                                #{formatted}
